Task #084
Step objective: Add notification support for newly classified action-required emails.
Chosen fix: Reuse the existing node-to-Core MQTT notification path and add per-message notification dedupe in Gmail message storage so notifications only fire once for new qualifying classifications.
What changed: Added boolean notification flags in Gmail message storage, added a reusable classified-email notification helper in NodeService, and wired action-required notifications into both local-model and AI-model classification updates.
Verification: `python -m py_compile src/*.py src/core/*.py src/providers/*.py src/providers/gmail/*.py tests/test_gmail_message_store.py tests/test_notifications.py` passed. Direct runtime smoke test could not run because this environment is missing Python dependencies such as `pydantic`.
###########
Task #085
Step objective: Extend the reusable classified-email notification path to order emails.
Chosen fix: Reuse the same boolean DB flag pattern and service helper from Task 084, adding only the `order` delivery mapping and regression coverage.
What changed: Enabled `order` notification emission with sender, subject, and confidence details, and added a matching test plus doc update.
Verification: `python -m py_compile src/*.py src/core/*.py src/providers/*.py src/providers/gmail/*.py tests/test_gmail_message_store.py tests/test_notifications.py` passed.
###########
#############
All Tasks completed 04/03/2026 15:41:43
#############
Task #105
Step objective: Start gateway extraction work by defining the active task, current prompt location, and direct AI/provider integration paths.
Chosen fix: Use the task workflow queue, preserve the detailed plan in `docs/task-details.md`, and begin implementation by extracting shared gateway boundaries from the existing service-layer HTTP logic instead of adding more ad hoc guards.
What changed: Marked Task 105 as `[WIP]`, confirmed runtime prompt JSON files currently live under `src/runtime_prompts/`, and mapped the current AI-node and Gmail/provider call surfaces that need to move behind modular gateways.
Verification: `rg` inventory completed for runtime prompts, AI-node prompt/execution routes, and Gmail/provider transport call sites. MQTT starting publish failed with `Connection Refused: not authorised`, so execution continued locally.
###########
Task #105
Step objective: Extract the first backend gateway unit and move prompt definitions into a runtime-owned folder.
Chosen fix: Add dedicated `AiNodeGateway` and `EmailProviderGateway` modules, route the existing service/provider manager remote calls through them, and relocate prompt JSON loading to `runtime/prompts` while preserving prompt contents exactly.
What changed: Added gateway modules under `src/node_backend/`, wired `NodeService`, `ProviderManager`, and the Gmail status scheduler to use the new gateways for AI-node prompt/execution calls and Gmail remote mailbox/token flows, changed the default prompt-definition directory to `runtime/prompts`, copied the existing prompt JSON definitions into `runtime/prompts/`, deleted the old `src/runtime_prompts/` copies, and updated prompt-location docs/schema references.
Verification: `python -m py_compile src/config.py src/node_backend/__init__.py src/node_backend/runtime.py src/node_backend/ai_gateway.py src/node_backend/email_provider_gateway.py src/node_backend/providers.py src/node_backend/scheduler.py src/service.py` passed.
###########
Task #105
Step objective: Add provider-call gating through the new email-provider gateway and expose it on the runtime page.
Chosen fix: Persist a separate `provider_calls_enabled` runtime setting, enforce it inside `EmailProviderGateway`, and make status/polling code fall back to cached/local data so disable mode stays quiet instead of turning into repeated errors.
What changed: Added `provider_calls_enabled` runtime state and settings handling, enforced provider-call disable checks inside `EmailProviderGateway`, updated Gmail status and scheduler flows to skip remote provider calls cleanly when disabled, added a second runtime toggle in the dashboard UI, and expanded the dashboard render test coverage to include the new toggle state.
Verification: `python -m py_compile src/node_backend/runtime.py src/node_models/runtime.py src/node_backend/email_provider_gateway.py src/node_backend/providers.py src/node_backend/scheduler.py src/service.py` passed. `npm --prefix frontend test -- --run src/features/dashboard/dashboard.test.jsx` passed.
###########
Task #086
Step objective: Add sender reputation storage based on DB ratings/signals.
Chosen fix: Add sender reputation records to the local Gmail storage layer so later aggregation and API tasks can reuse one persisted shape instead of duplicating sender/domain score data.
What changed: Reused the existing sender reputation Pydantic models, added a `gmail_sender_reputation` SQLite table to the Gmail message store, implemented reusable upsert/get/list helpers for sender email and domain records, and added store-level regression coverage for persistence and recency ordering.
Verification: `python -m py_compile src/providers/gmail/models.py src/providers/gmail/message_store.py tests/test_gmail_message_store.py` passed. `python -m pytest tests/test_gmail_message_store.py` could not run because `pytest` is not installed in this environment.
###########
Task #087
Step objective: Build a reusable sender reputation aggregation and update flow.
Chosen fix: Compute sender reputation from the message/spamhaus/classification data already stored locally, then persist the derived sender email and sender domain records through the new store helpers so runtime, training, and UI code can all read the same results.
What changed: Added a reusable Gmail sender reputation aggregation module that derives sender email/domain records from stored messages, local classifications, and Spamhaus checks, added store helpers for replacing account reputations and listing Spamhaus checks, and wired the refresh flow into Gmail fetches, Spamhaus updates, manual review saves, semi-auto review saves, and runtime local/AI classification passes.
Verification: `python -m py_compile src/providers/gmail/reputation.py src/providers/gmail/message_store.py src/providers/gmail/adapter.py src/service.py tests/test_gmail_adapter.py tests/test_gmail_message_store.py` passed. Runtime tests still could not be executed because `pytest` is not installed in this environment.
###########
Task #088
Step objective: Expose sender reputation in backend APIs.
Chosen fix: Add sender reputation summary/detail read methods in the Gmail adapter and surface them through the existing service/UI API structure so the dashboard and training views can consume reputation data without querying the DB directly.
What changed: Added sender reputation summary/detail API methods in the Gmail adapter, exposed sender reputation summary on the Gmail status and training payloads, added dedicated `/api/gmail/reputation` and `/api/gmail/reputation/detail` routes, and covered the new payload shape with UI API regression tests.
Verification: `python -m py_compile src/providers/gmail/adapter.py src/providers/gmail/reputation.py src/providers/gmail/message_store.py src/service.py src/main.py tests/test_ui_api.py tests/test_gmail_adapter.py` passed. `python -m pytest tests/test_ui_api.py tests/test_gmail_adapter.py` could not run because `pytest` is not installed in this environment.
###########
Task #089
Step objective: Show sender reputation in the UI.
Chosen fix: Reuse the new Gmail status/training sender reputation payloads and add a lightweight inspectable detail view in the existing Gmail/training screens so operators can see state, scores, and why a sender is classified that way.
What changed: Added a shared sender reputation panel to the Gmail dashboard and Training page, surfaced summary counts and top sender/domain records from the new backend payloads, and added inspect actions that load detailed inputs and recent contributing messages without leaving the current screen.
Verification: `npm run build` passed in `frontend/`.
###########
Task #090
Step objective: Use sender reputation in classification and notifications.
Chosen fix: Thread sender reputation into the local/runtime classification helper outputs and add reputation state/rating context to operator-facing notifications and debug surfaces so risky senders are easier to spot.
What changed: Added sender reputation context to action-required/order notifications, appended sender reputation state/rating signals to AI-node email classification inputs when available, surfaced sender reputation in runtime classification results, and documented the operator-visible behavior in a dedicated sender reputation note.
Verification: `python -m py_compile src/service.py tests/test_notifications.py tests/test_ui_api.py` passed. `npm run build` passed in `frontend/`.
###########
Task #091
Step objective: Fetch six months for initial Gmail learning.
Chosen fix: Extend the initial-learning Gmail fetch query window from three months to six months so the first learning import matches the existing six-month local retention policy.
What changed: Updated the Gmail mailbox client so `initial_learning` fetches six months of mail, refreshed the mailbox-client query regression to assert the exact six-month window, and updated the fetch-status documentation to match the new initial learning range.
Verification: `python -m py_compile src/providers/gmail/mailbox_client.py tests/test_gmail_mailbox_client.py` passed.
###########
Task #092
Step objective: Schedule hourly batch classification for newest unclassified emails.
Chosen fix: Reuse the existing runtime batch classification flow inside the minute-based Gmail scheduler loop, adding a persisted hourly slot key so the batch job only runs once per hour around `:00` even if the scheduler checks multiple times.
What changed: Added persisted hourly batch slot tracking to runtime state, wired the Gmail scheduler loop to trigger the existing 100-message batch classification flow once per hour near minute `00`, and added regression coverage for the once-per-slot behavior.
Verification: `python -m py_compile src/models.py src/service.py src/providers/gmail/mailbox_client.py tests/test_gmail_mailbox_client.py tests/test_ui_api.py` passed. `python -m pytest ...` still could not run because `pytest` is not installed in this environment.
###########
Task #093
Step objective: Add reusable non-webmail business-domain reputation records.
Chosen fix: Extend the existing sender reputation aggregation to derive a third `business_domain` entity while filtering out common mailbox-provider domains, so domain grouping can reuse the same persisted reputation flow.
What changed: Added business-domain extraction and mailbox-provider exclusions in the reputation builder, extended persisted reputation records with `group_domain` and `derived_rating`, and covered the new entity type in storage, adapter, and API tests.
Verification: `python -m py_compile src/providers/gmail/models.py src/providers/gmail/reputation.py src/providers/gmail/message_store.py src/providers/gmail/adapter.py src/service.py src/main.py tests/test_gmail_message_store.py tests/test_gmail_adapter.py tests/test_ui_api.py` passed.
###########
Task #094
Step objective: Group the sender reputation page by domain with collapsible sections.
Chosen fix: Reuse the existing training-style reputation page and group records client-side by `group_domain`, keeping the summary record visible in each collapsed section.
What changed: Added grouped/collapsible domain sections in the `/training/reputation` page, kept the Gmail dashboard summary-only, and added supporting styles for grouped sender reputation cards.
Verification: `npm run build` passed in `frontend/`.
###########
Task #095
Step objective: Add manual sender and domain reputation rating.
Chosen fix: Add a focused manual-rating API and hook it into the selected-record panel on the reputation page so operator overrides persist and update the effective score without bypassing the derived signal model.
What changed: Added backend manual-rating persistence and API support, preserved manual ratings across reputation refreshes, and added reputation-page controls for setting and clearing operator ratings with notes.
Verification: `python -m py_compile src/providers/gmail/models.py src/providers/gmail/reputation.py src/providers/gmail/message_store.py src/providers/gmail/adapter.py src/service.py src/main.py tests/test_gmail_message_store.py tests/test_gmail_adapter.py tests/test_ui_api.py` passed. `npm run build` passed in `frontend/`.
###########
Task #096
Step objective: Add risk-level filtering to the reputation list.
Chosen fix: Filter the grouped reputation page client-side by the existing risk buckets so filtering works naturally with the collapsible domain sections and does not require a separate backend endpoint.
What changed: Added trusted/neutral/risky/blocked filter controls to the sender reputation page and kept the grouped domain summaries compatible with the active filter.
Verification: `npm run build` passed in `frontend/`.
###########
Task #097
Step objective: Redesign runtime prompt registration around real prompt-service sync instead of blind prompt posts.
Chosen fix: Move prompt registration ownership into the backend, load local prompt definitions from a dedicated JSON folder, compare them against the AI-node prompt-service listing, and only register missing prompts while retiring and re-registering outdated versions.
What changed: Added folder-scanned prompt JSON loading in `src/runtime_prompts`, added a dedicated `/api/runtime/prompts/sync` backend route, persisted the last prompt-sync target and weekly sync slot in runtime state, implemented missing/unchanged/replaced prompt sync behavior against the AI-node prompt-service list and lifecycle endpoints, wired the minute scheduler to perform the sync once per week, and removed the dummy frontend registration payload in favor of the real sync API.
Verification: `python -m py_compile src/config.py src/models.py src/main.py src/service.py tests/helpers.py tests/test_ui_api.py` passed. `npm run build` passed in `frontend/`.
###########
Task #098
Step objective: Extract the hardcoded email-classifier prompt into JSON.
Chosen fix: Store the prompt registration body and node-local execution metadata together in a dedicated JSON definition file so registration and execution read the same source of truth.
What changed: Added `src/runtime_prompts/prompt.email.classifier.json`, removed the hardcoded prompt-registration builder from `NodeService`, and updated the runtime email-classifier execution path to read the prompt id, version, timeout, and structured output schema from the JSON definition.
Verification: `python -m py_compile src/config.py src/models.py src/main.py src/service.py tests/helpers.py tests/test_ui_api.py` passed. `npm run build` passed in `frontend/`.
###########
Task #099
Step objective: Run the AI action-decision prompt for newly classified `action_required` and `order` emails.
Chosen fix: Reuse the existing AI-node direct execution pattern with a second runtime prompt definition, persist the action-decision response on the Gmail message row, and execute it only for the qualifying classification labels so later notification/UI work can reuse the stored decision.
What changed: Added `prompt.email.action_decision` runtime JSON with structured-output schema, extended Gmail stored-message persistence with action-decision payload/version/timestamp fields, implemented an AI action-decision execution helper that builds prompt-template inputs from stored Gmail message metadata, and cached the returned decision on the message to avoid re-running the same prompt version unnecessarily.
Verification: `python -m py_compile src/service.py src/providers/gmail/models.py src/providers/gmail/message_store.py tests/helpers.py tests/test_notifications.py tests/test_gmail_message_store.py tests/test_ui_api.py` passed. Prompt JSON validation passed for `src/runtime_prompts/prompt.email.classifier.json` and `src/runtime_prompts/prompt.email.action_decision.json`.
###########
Task #100
Step objective: Replace the old classified-email notification body with the action-decision reply in a readable operator format.
Chosen fix: Keep the existing notification dedupe and delivery path, but render the stored AI decision into a readable summary with recommended actions, urgency, time/tracking hints, and human-review visibility, while gracefully falling back to the old debug-style message if no action-decision response is available.
What changed: Converted the classified-email notification hook to async so it can obtain the action-decision result, updated local/AI/manual classification call sites to await it, and reformatted action-required/order notifications to include AI summary, recommended actions, deadlines, shipment hints, and human-review guidance instead of the old minimal sender/subject/confidence lines.
Verification: `python -m py_compile src/service.py src/providers/gmail/models.py src/providers/gmail/message_store.py tests/helpers.py tests/test_notifications.py tests/test_gmail_message_store.py tests/test_ui_api.py` passed.
###########
Task #101
Step objective: Add a dedicated dashboard screen for scheduled tasks with one full-width card.
Chosen fix: Expose a backend `scheduled_tasks` summary from `ui_bootstrap` so the frontend can render a stable table of scheduler-driven jobs without duplicating schedule calculations in the browser.
What changed: Added scheduled-task summary generation in `NodeService` for Gmail fetch windows, hourly batch classification, and weekly prompt sync; persisted last scheduled timestamps for the batch classifier and weekly prompt sync; exposed the summary on the bootstrap payload; and added a new `Scheduled Tasks` dashboard section with a single full-width table card showing schedule, status, last execution, next execution, reason, slot key, and detail.
Verification: `python -m py_compile src/models.py src/service.py tests/test_ui_api.py` passed. `npm run build` passed in `frontend/`.
###########
Task #105
Step objective: Verify runtime prompt loading uses the new runtime-owned prompt directory.
Chosen fix: Add a targeted regression test that points `PROMPT_DEFINITION_DIR` at a runtime folder, disables the legacy fallback path, and verifies `NodeService` loads the prompt JSON from that configured runtime location.
What changed: Added a prompt-loader regression test in `tests/test_ui_api.py` and updated `docs/runtime.md` to note that runtime prompt loading follows `PROMPT_DEFINITION_DIR`, which defaults to `runtime/prompts`.
Verification: `python -m py_compile tests/test_ui_api.py src/node_backend/runtime.py src/service.py src/config.py` passed. `python -m pytest tests/test_ui_api.py -k runtime_prompt_loader_reads_from_runtime_prompt_directory` could not run because `pytest` is not installed in this environment.
###########
Task #105
Step objective: Route the ORDER Phase 1 full-message fetch through `EmailProviderGateway`.
Chosen fix: Change the Phase 1 processor to accept a fetch callback instead of a Gmail adapter, then pass `EmailProviderGateway.gmail_fetch_full_message_payload` from `NodeService` so provider-disable enforcement applies to the ORDER normalization flow too.
What changed: Updated `src/providers/gmail/order_flow.py` and `src/service.py` so ORDER Phase 1 uses the provider gateway for remote full-message payload fetches, aligned the `tests/test_gmail_order_flow.py` test double with the new callback signature, and added a regression test in `tests/test_ui_api.py` that confirms provider-disabled mode blocks the remote fetch attempt.
Verification: `python -m py_compile src/providers/gmail/order_flow.py src/service.py tests/test_ui_api.py tests/test_gmail_order_flow.py` passed.
###########
Task #105
Step objective: Add gateway-level regression coverage for the new outbound boundaries.
Chosen fix: Add a dedicated test file that exercises the most important gateway contracts directly: AI prompt-read 400 handling, AI disabled-mode rejection, and provider disabled-mode rejection before any adapter fetch runs.
What changed: Added `tests/test_gateway_boundaries.py` covering `AiNodeGateway.get_prompt_service`, `AiNodeGateway.execute_direct`, and `EmailProviderGateway.gmail_fetch_full_message_payload`.
Verification: `python -m py_compile tests/test_gateway_boundaries.py src/node_backend/ai_gateway.py src/node_backend/email_provider_gateway.py` passed.
###########
Task #105
Step objective: Document the canonical gateway boundaries and expand direct gateway contract coverage.
Chosen fix: Update the runtime doc to name `AiNodeGateway` and `EmailProviderGateway` as the supported outbound boundaries, then add one more direct test for AI URL normalization and successful payload passthrough.
What changed: Updated `docs/runtime.md` with an outbound-boundaries section and expanded `tests/test_gateway_boundaries.py` with a success-path `AiNodeGateway.execute_direct` contract test.
Verification: `python -m py_compile tests/test_gateway_boundaries.py` passed.
###########
Task #105
Step objective: Add provider-gateway success-path passthrough coverage.
Chosen fix: Add one direct `EmailProviderGateway` test that verifies the enabled path forwards the fetch call to the adapter and returns the provider payload unchanged.
What changed: Expanded `tests/test_gateway_boundaries.py` with an enabled-mode `gmail_fetch_full_message_payload` passthrough test.
Verification: `python -m py_compile tests/test_gateway_boundaries.py` passed.
###########
Task #226
Step objective: Define the pattern generation request contract.
Chosen fix: Add a new `src/email_node/patterns/` package with a strict Pydantic request model that forbids extra keys, validates required text fields, normalizes `expected_label`, and applies the requested defaults for `template_version`, `body_html`, and `links_json`.
What changed: Added `src/email_node/__init__.py`, `src/email_node/patterns/__init__.py`, `src/email_node/patterns/pattern_generation_request.py`, and `tests/test_pattern_generation_request.py` covering valid input, missing required fields, empty `body_text`, and empty template/profile IDs.
Verification: `python -m py_compile src/email_node/__init__.py src/email_node/patterns/__init__.py src/email_node/patterns/pattern_generation_request.py tests/test_pattern_generation_request.py` passed. A direct runtime import check could not complete in this shell because `pydantic` is not installed in the default interpreter environment.
###########
Task #227
Step objective: Define the pattern generation response contract.
Chosen fix: Add a strict Pydantic Phase 4 template model with forbidden extra top-level keys, a required `match.vendor_identity`, method-discriminated extract rules for the supported registry methods, and focused validation tests for valid and invalid shapes.
What changed: Added `src/email_node/patterns/pattern_generation_response.py`, exported it from `src/email_node/patterns/__init__.py`, and added `tests/test_pattern_generation_response.py` covering valid templates, extra top-level keys, missing `vendor_identity`, and malformed extract rules.
Verification: `python -m py_compile src/email_node/patterns/__init__.py src/email_node/patterns/pattern_generation_response.py tests/test_pattern_generation_response.py` passed.
###########
Task #228
Step objective: Implement the prompt service client for pattern creation.
Chosen fix: Add a standalone async client that loads the local prompt definition, builds the same `/api/execution/direct` request shape used elsewhere in the repo, enforces JSON-only output, and retries exactly once on JSON parse failures.
What changed: Added `src/email_node/patterns/pattern_generation_client.py`, exported the client from `src/email_node/patterns/__init__.py`, and added `tests/test_pattern_generation_client.py` covering success, one retry after markdown-wrapped output, and failure after retry exhaustion.
Verification: `python -m py_compile src/email_node/patterns/__init__.py src/email_node/patterns/pattern_generation_client.py tests/test_pattern_generation_client.py` passed.
###########
Task #229
Step objective: Implement the JSON parsing and validation pipeline.
Chosen fix: Wrap the new client with a small pipeline that normalizes nullable collection fields into schema-safe values, validates the result through the strict response contract, and raises deterministic pipeline errors on client failures or schema mismatches.
What changed: Added `src/email_node/patterns/pattern_generation_pipeline.py`, exported it from `src/email_node/patterns/__init__.py`, and added `tests/test_pattern_generation_pipeline.py` covering valid output, null-array/post-process normalization, and schema-mismatch failure handling.
Verification: `python -m py_compile src/email_node/patterns/__init__.py src/email_node/patterns/pattern_generation_pipeline.py tests/test_pattern_generation_pipeline.py` passed.
###########
Task #230
Step objective: Implement the draft template file writer.
Chosen fix: Add a small writer class that saves validated response models as pretty JSON under `src/email_node/patterns/draft`, rejects accidental overwrites by default, and allows them only when explicitly requested.
What changed: Added `src/email_node/patterns/pattern_generation_writer.py`, exported it from `src/email_node/patterns/__init__.py`, and added `tests/test_pattern_generation_writer.py` covering normal writes, overwrite rejection, and allowed overwrite behavior.
Verification: `python -m py_compile src/email_node/patterns/__init__.py src/email_node/patterns/pattern_generation_writer.py tests/test_pattern_generation_writer.py` passed.
###########
Task #231
Step objective: Implement the pattern generation orchestration service.
Chosen fix: Add a thin service layer that composes the pipeline and writer, returns a stable success payload on completion, and raises a dedicated service error when the pipeline or writer fails.
What changed: Added `src/email_node/patterns/pattern_generation_service.py`, exported it from `src/email_node/patterns/__init__.py`, and added `tests/test_pattern_generation_service.py` covering end-to-end success and structured failure propagation.
Verification: `python -m py_compile src/email_node/patterns/__init__.py src/email_node/patterns/pattern_generation_service.py tests/test_pattern_generation_service.py` passed.
###########
Task #232
Step objective: Add the pattern generation API endpoint.
Chosen fix: Expose a small router in `src/email_node/api/patterns.py` that accepts the request contract, delegates to a new `NodeService.generate_pattern_template()` entrypoint, and maps generation failures to a deterministic 400 response.
What changed: Added `src/email_node/api/patterns.py` and `src/email_node/api/__init__.py`, wired the route into `src/main.py`, added `NodeService.generate_pattern_template()` in `src/service.py`, and added `tests/test_pattern_generation_api.py` covering success and failure responses.
Verification: `python -m py_compile src/main.py src/service.py src/email_node/api/__init__.py src/email_node/api/patterns.py tests/test_pattern_generation_api.py` passed.
###########
Task #233
Step objective: Add the CLI tool for pattern generation.
Chosen fix: Add a lightweight async CLI wrapper around the new pattern-generation service so sample email JSON files can be submitted locally without going through the API layer, while keeping the service factory injectable for tests.
What changed: Added `scripts/generate_pattern.py` with `run_cli()` and `main()` entrypoints, and added `tests/test_generate_pattern_script.py` covering success and error output behavior.
Verification: `python -m py_compile scripts/generate_pattern.py tests/test_generate_pattern_script.py` passed.
###########
Task #234
Step objective: Add logging and diagnostics to the pattern generation flow.
Chosen fix: Add structured logs around safe request metadata in the client, truncated raw AI output with an opt-in full-response debug mode, schema validation failures in the pipeline, and the final saved file path in the service.
What changed: Updated `src/email_node/patterns/pattern_generation_client.py`, `src/email_node/patterns/pattern_generation_pipeline.py`, and `src/email_node/patterns/pattern_generation_service.py` with structured diagnostics while avoiding body-text logging by default.
Verification: `python -m py_compile src/email_node/patterns/pattern_generation_client.py src/email_node/patterns/pattern_generation_pipeline.py src/email_node/patterns/pattern_generation_service.py` passed.
###########
Task #235
Step objective: Add initial aggregate test coverage for the pattern generation flow.
Chosen fix: Add a single end-to-end oriented test file that exercises the real client, pipeline, service, and writer stack with a mock AI transport and temporary output directory.
What changed: Added `tests/test_pattern_generation.py` covering valid generation flow, invalid JSON response failure, schema validation failure, and file write success.
Verification: `python -m py_compile tests/test_pattern_generation.py` passed.
###########
Task #316
Step objective: Document the multi-family rollout plan for ORDER, ACTION_REQUIRED, FINANCIAL, INVOICE, SECURITY, and SHIPMENT.
Chosen fix: Add one rollout-focused doc that captures family maturity, readiness gaps, rollout order, and the mailbox-sampling dependency, then link it from the shared architecture document so it stays discoverable.
What changed: Added `docs/multi-family-rollout-plan.md`, linked it from `docs/shared-flow-family-architecture.md`, removed Task 316 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/email_node/shared_pipeline_core/families.py` passed.
###########
Task #317
Step objective: Sample 20-30 FINANCIAL emails from the Gmail DB and derive taxonomy inputs from scrubbed content.
Chosen fix: Add one reusable mailbox-sampling script that reuses the real Phase 1, Phase 2, and Phase 3 family logic against stored Gmail payloads, including a snippet-backed fallback for list-style DB payloads, and write per-family JSON and Markdown artifacts under the family runtime directory.
What changed: Added `scripts/sample_flow_family_mailbox.py`, generated `runtime/flow_families/financial/analysis/financial_mailbox_sample.json`, generated `runtime/flow_families/financial/analysis/financial_mailbox_sample.md`, removed Task 317 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile scripts/sample_flow_family_mailbox.py` passed. `PYTHONPATH=src ./.venv/bin/python scripts/sample_flow_family_mailbox.py financial --limit 30` passed.
###########
Task #318
Step objective: Sample 20-30 INVOICE emails from the Gmail DB and derive taxonomy inputs from scrubbed content.
Chosen fix: Reuse the new mailbox-sampling script and persist the INVOICE sample under the family runtime analysis directory so the later taxonomy and prompt tasks can work from the same artifact format as FINANCIAL.
What changed: Generated `runtime/flow_families/invoice/analysis/invoice_mailbox_sample.json`, generated `runtime/flow_families/invoice/analysis/invoice_mailbox_sample.md`, removed Task 318 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `PYTHONPATH=src ./.venv/bin/python scripts/sample_flow_family_mailbox.py invoice --limit 30` passed.
###########
Task #319
Step objective: Sample 20-30 SECURITY emails from the Gmail DB and derive taxonomy inputs from scrubbed content.
Chosen fix: Reuse the same mailbox-sampling script so SECURITY sample artifacts are directly comparable to FINANCIAL and INVOICE, then persist them under the family runtime analysis directory for the later taxonomy-tuning pass.
What changed: Generated `runtime/flow_families/security/analysis/security_mailbox_sample.json`, generated `runtime/flow_families/security/analysis/security_mailbox_sample.md`, removed Task 319 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `PYTHONPATH=src ./.venv/bin/python scripts/sample_flow_family_mailbox.py security --limit 30` passed.
###########
Task #320
Step objective: Sample 20-30 SHIPMENT emails from the Gmail DB and derive taxonomy inputs from scrubbed content.
Chosen fix: Reuse the mailbox-sampling script one more time for the SHIPMENT label so all four new-family sample artifacts share the same runtime execution path, aggregate format, and output location.
What changed: Generated `runtime/flow_families/shipment/analysis/shipment_mailbox_sample.json`, generated `runtime/flow_families/shipment/analysis/shipment_mailbox_sample.md`, removed Task 320 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `PYTHONPATH=src ./.venv/bin/python scripts/sample_flow_family_mailbox.py shipment --limit 30` passed.
###########
Task #321
Step objective: Add sampled-family analysis artifacts and notes for FINANCIAL, INVOICE, SECURITY, and SHIPMENT.
Chosen fix: Keep the raw JSON and Markdown artifacts under each family runtime directory, then add one doc-level synthesis that compares the four families and turns the repeated domains, phrases, misses, and likely taxonomy additions into explicit guidance for the next tuning tasks.
What changed: Added `docs/family-mailbox-sample-analysis.md`, linked it from `docs/multi-family-rollout-plan.md`, removed Task 321 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Artifact-backed doc review completed against the sampled family JSON and Markdown files under `runtime/flow_families/*/analysis/`.
###########
Task #322
Step objective: Evaluate whether one generic family template-creation prompt can cover multi-family AI template generation.
Chosen fix: Compare the current ORDER and ACTION_REQUIRED prompt contracts, the shared request/response models, and the sampled-family findings to decide whether family differences live in the outer prompt mechanics or in family-owned guidance blocks.
What changed: Added `docs/family-template-prompt-strategy.md`, recorded that the shared prompt contract is viable for the current family set, removed Task 322 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Contract review completed against `runtime/prompts/prompt.email.order_pattern_template_creation.json`, `runtime/prompts/prompt.email.action_required_pattern_template_creation.json`, `src/email_node/patterns/pattern_generation_request.py`, and `src/email_node/patterns/pattern_generation_response.py`.
###########
Task #323
Step objective: Define a generic family template-creation prompt contract if the shared prompt is viable.
Chosen fix: Add one shared prompt artifact with family-specific schema-version and field-guidance rules inside the prompt body, then route all supported labels to that prompt through the existing client instead of introducing a bigger request-model refactor right now.
What changed: Added `runtime/prompts/prompt.email.family_pattern_template_creation.json`, updated `src/email_node/patterns/pattern_generation_client.py` to route ORDER, SHIPMENT, ACTION_REQUIRED, FINANCIAL, INVOICE, and SECURITY to the shared prompt, updated `tests/test_pattern_generation_client.py`, updated `docs/family-template-prompt-strategy.md`, removed Task 323 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: JSON parse check for the new prompt passed. `python -m py_compile src/email_node/patterns/pattern_generation_client.py tests/test_pattern_generation_client.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_pattern_generation_client.py tests/test_pattern_generation_request.py -q` passed.
###########
Task #324
Step objective: Create family-specific AI template-creation prompts if a shared prompt is not viable.
Chosen fix: Mark the conditional task as not needed because Task 322 concluded the shared contract was viable and Task 323 implemented it.
What changed: Removed Task 324 from `docs/New_task.txt` and recorded the conditional no-op completion in `docs/completed.txt`.
Verification: Shared prompt decision and implementation already verified in Tasks 322 and 323.
###########
Task #325
Step objective: Document the prompt strategy for multi-family AI template generation.
Chosen fix: Use the new prompt-strategy doc as the canonical decision record, then update the main pattern-generation doc so the live shared prompt artifact, supported labels, and legacy prompt status are visible from the primary operator/developer entrypoint.
What changed: Updated `docs/pattern-generation.md`, removed Task 325 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `docs/family-template-prompt-strategy.md`, `docs/pattern-generation.md`, and the live prompt artifact `runtime/prompts/prompt.email.family_pattern_template_creation.json`.
###########
Task #326
Step objective: Audit HexeEmail scheduled tasks against the 2026-04-05 background-tasks-and-internal-scheduler standard and document the repo-specific compliance gaps.
Chosen fix: Compare the new scheduler standard against the live scheduler owner, current scheduled-task snapshot, current UI columns, and current scheduler docs, then capture the resulting compliance gaps in one repo-specific audit document that maps directly to Tasks 327 through 338.
What changed: Added `docs/scheduler-standard-gap-audit.md`, removed Task 326 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Audit review completed against `/home/dan/Projects/Hexe/docs/standards/Node/background-tasks-and-internal-scheduler-standard.md`, `src/node_backend/scheduler.py`, `frontend/src/features/dashboard/ScheduledTasksSection.jsx`, and `docs/scheduler-and-background-tasks.md`.
###########
Task #327
Step objective: Create an explicit scheduler task registry with stable task identity, task kind, schedule name, schedule detail, enabled state, and owner mapping for all recurring work.
Chosen fix: Add a first-class scheduler task registry in `BackgroundTaskManager`, enrich scheduled-task snapshot metadata from that registry model, and lock the bootstrap contract down with focused UI API coverage.
What changed: Updated `src/node_backend/scheduler.py`, `tests/test_ui_api.py`, and `docs/scheduler-and-background-tasks.md`, removed Task 327 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/node_backend/scheduler.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry' -q` passed.
###########
Task #328
Step objective: Add mandatory baseline recurring tasks for heartbeat, telemetry, and operational MQTT health with explicit ownership, persisted state, and operator visibility.
Chosen fix: Model the three baseline tasks explicitly in `BackgroundTaskManager`, add lightweight persisted scheduler task state for them, hook heartbeat updates into the existing MQTT callbacks, and expose the new tasks through the bootstrap scheduled-task snapshot and docs.
What changed: Updated `src/node_backend/scheduler.py`, `src/service.py`, `tests/test_ui_api.py`, and `docs/scheduler-and-background-tasks.md`, removed Task 328 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry' -q` passed.
###########
Task #329
Step objective: Persist structured scheduler task state per recurring task including current status, enabled state, last start, last completion, last success, last failure, next run, and last error.
Chosen fix: Extend `runtime_task_state.scheduler_task_states` from the baseline task layer into the rest of the explicit scheduler registry, add helper methods for running/success/failure/idle transitions, and wire those state updates into the recurring loop and scheduled execution paths that already exist today.
What changed: Updated `src/node_backend/scheduler.py`, `src/service.py`, `tests/test_ui_api.py`, and `docs/scheduler-and-background-tasks.md`, removed Task 329 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry' -q` passed.
###########
Task #330
Step objective: Refactor existing recurring loops into registry-backed scheduled tasks, including finalize polling, Gmail status polling, Gmail fetch scheduler, hourly batch classification, weekly prompt sync, and monthly authorize.
Chosen fix: Introduce a registry-backed scheduled-task entry builder and use the explicit registry metadata as the source of truth for the operator-visible scheduled-task snapshot, while keeping the loop/task-state updates wired to the same task ids.
What changed: Updated `src/node_backend/scheduler.py`, `docs/scheduler-and-background-tasks.md`, removed Task 330 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry' -q` passed.
###########
Task #331
Step objective: Distinguish node-local recurring work, provider-recurring work, and any Core-leased work in scheduler task metadata and operator-facing labels.
Chosen fix: Keep the API metadata on `kind`, but switch the Scheduled Tasks dashboard section to render operator-friendly kind labels with the explicit task owner instead of the older `group` field.
What changed: Updated `frontend/src/features/dashboard/ScheduledTasksSection.jsx`, `frontend/src/features/dashboard/dashboard.test.jsx`, and `docs/scheduler-and-background-tasks.md`, removed Task 331 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `npm --prefix frontend test -- --run src/features/dashboard/dashboard.test.jsx` passed. `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed.
###########
Task #332
Step objective: Align scheduler startup, readiness gating, and shutdown handling with the standard, including restored state, trust prerequisites, and explicit stop/finalization rules.
Chosen fix: Gate provider-affecting recurring tasks behind trusted operational readiness, re-run that gate whenever operational readiness changes, and record explicit stopped state for long-lived loop-backed tasks during scheduler shutdown.
What changed: Updated `src/node_backend/scheduler.py`, `src/node_backend/governance.py`, `tests/test_ui_api.py`, and `docs/scheduler-and-background-tasks.md`, removed Task 332 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/node_backend/scheduler.py src/node_backend/governance.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry or defers_provider_loops' -q` passed.
###########
Task #333
Step objective: Update the scheduled task API/bootstrap payloads to expose the standard task fields and friendly schedule labels needed by the operator UI.
Chosen fix: Extend each scheduled-task payload row with the structured scheduler-state fields (`last_started_at`, `last_completed_at`, `last_success_at`, `last_failure_at`, `last_error`) plus a friendly `schedule_label`, while keeping the older compatibility fields in place for the transition.
What changed: Updated `src/node_backend/scheduler.py`, `src/service.py`, and `tests/test_ui_api.py`, removed Task 333 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry or defers_provider_loops' -q` passed.
###########
Task #334
Step objective: Update the Scheduled Tasks dashboard card to match the standard column model: Task, Kind, Schedule, Status, Last Success, Last Failure, Next Run, and Last Error.
Chosen fix: Switch the Scheduled Tasks table to the standard columns and render the new API fields directly, while keeping the task kind label and owner presentation from Task 331.
What changed: Updated `frontend/src/features/dashboard/ScheduledTasksSection.jsx`, `frontend/src/features/dashboard/dashboard.test.jsx`, removed Task 334 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `npm --prefix frontend test -- --run src/features/dashboard/dashboard.test.jsx` passed. `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed.
###########
Task #335
Step objective: Add the schedule catalog or legend sorted from shortest to longest duration, with interval_seconds sorted last when present.
Chosen fix: Add a shared schedule-template sort order in the backend, mirror that ordering in the frontend fallback legend, and lock the ordering down in the scheduled-task API test.
What changed: Updated `src/node_backend/scheduler.py`, `frontend/src/App.jsx`, `tests/test_ui_api.py`, and `docs/scheduler-and-background-tasks.md`, removed Task 335 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry or defers_provider_loops' -q` passed.
###########
Task #336
Step objective: Add consistent scheduled-task status semantics and UI badges so idle/stopped are orange, failing is red, and running/scheduled/healthy are green with running darker.
Chosen fix: Update the scheduler status tone mapping in the frontend to use warning for idle/inactive/stopped, danger for failing, success-strong for running, and success for the remaining healthy scheduler states, then add the darker green badge style and dashboard coverage.
What changed: Updated `frontend/src/App.jsx`, `frontend/src/styles.css`, `frontend/src/features/dashboard/dashboard.test.jsx`, and `docs/scheduler-and-background-tasks.md`, removed Task 336 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `npm --prefix frontend test -- --run src/features/dashboard/dashboard.test.jsx` passed. `python -m py_compile src/node_backend/scheduler.py src/service.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_ui_api.py -k 'scheduled_tasks or task_registry or defers_provider_loops' -q` passed.
###########
Task #337
Step objective: Add scheduler regression coverage for task registry loading, task-state persistence, baseline recurring tasks, startup/shutdown behavior, and API/UI scheduled-task rendering.
Chosen fix: Add a focused scheduler-standard backend regression file for legend ordering, startup state initialization, and shutdown state transitions, while keeping the scheduled-task bootstrap/API assertions in `tests/test_ui_api.py` and the rendering checks in the dashboard frontend test.
What changed: Added `tests/test_scheduler_standard.py`, updated `tests/test_ui_api.py`, and used the existing dashboard rendering test as the UI side of the scheduler regression surface, removed Task 337 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: `python -m py_compile tests/test_scheduler_standard.py src/node_backend/scheduler.py src/node_backend/governance.py src/service.py tests/test_ui_api.py` passed. `PYTHONPATH=src ./.venv/bin/python -m pytest tests/test_scheduler_standard.py tests/test_ui_api.py -k 'scheduled_tasks or task_registry or defers_provider_loops or scheduler' -q` passed. `npm --prefix frontend test -- --run src/features/dashboard/dashboard.test.jsx` passed.
###########
Task #338
Step objective: Update scheduler and runtime docs to describe the standardized scheduled-task model, baseline tasks, ownership boundaries, and readiness interaction.
Chosen fix: Expand the scheduler reference doc with the final operator rules, then update the repo-level runtime doc and docs index so the standardized scheduler model is documented from both the scheduler-specific and runtime-overview entrypoints.
What changed: Updated `docs/scheduler-and-background-tasks.md`, `docs/runtime.md`, and `docs/index.md`, removed Task 338 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `docs/scheduler-and-background-tasks.md`, `docs/runtime.md`, `docs/index.md`, and the live scheduler implementation in `src/node_backend/scheduler.py`.
###########
Task #339
Step objective: Create a comprehensive flow-family architecture document covering the end-to-end shared pipeline model, per-phase responsibilities, shared-core boundaries, and family-specific extension points.
Chosen fix: Rewrite the shared architecture reference around the live phase order, shared contracts, family extension points, runtime layout, and current maturity of each family so it works as the top-level map for the new family docs instead of just a migration note.
What changed: Rewrote `docs/shared-flow-family-architecture.md`, removed Task 339 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `src/email_node/shared_pipeline_core/pipeline.py`, `src/email_node/shared_pipeline_core/decision.py`, `src/email_node/shared_pipeline_core/persistence.py`, and the live family YAML/runtime files.
###########
Task #340
Step objective: Document the ORDER family in detail, including profile detection, template execution, probation flow, decisioning, persistence, and downstream action behavior.
Chosen fix: Add one dedicated ORDER reference that explains the live taxonomy, Phase 2-4 behavior, probation lifecycle, decision model, persistence paths, downstream actions, and runtime switches without mixing it into the broader architecture doc.
What changed: Added `docs/order-family-reference.md`, removed Task 340 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `src/email_node/flow_families/order/runtime.py`, `runtime/flow_families/order/family.yaml`, and the current ORDER report artifacts under `runtime/order_flow_logs/ad_hoc_reports/`.
###########
Task #341
Step objective: Document the ACTION_REQUIRED family in detail, including taxonomy, profile detection, template strategy, fallback behavior, and downstream action behavior.
Chosen fix: Add a dedicated ACTION_REQUIRED reference that captures the expanded taxonomy, AI probation shape, current template limitations, placeholder actions, and the current gap between the family plumbing and active template coverage.
What changed: Added `docs/action-required-family-reference.md`, removed Task 341 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `src/email_node/flow_families/action_required/runtime.py`, `runtime/flow_families/action_required/family.yaml`, and the sampled ACTION_REQUIRED report artifacts.
###########
Task #342
Step objective: Document the planned FINANCIAL, INVOICE, SECURITY, and SHIPMENT families in detail, including intended taxonomy model, expected detection patterns, and planned action surfaces.
Chosen fix: Group the four newer families into a single planned-family reference that records their live YAML taxonomy, current placeholder action surfaces, smoke-tested runtime status, and family-specific quirks like intake overrides.
What changed: Added `docs/planned-family-references.md`, removed Task 342 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against the `runtime/flow_families/{financial,invoice,security,shipment}/family.yaml` files, their runtime modules, and the mailbox sample artifacts under each family analysis directory.
###########
Task #343
Step objective: Create a family-by-family action matrix that lists every supported and planned downstream action, the gating conditions for each action, and the runtime switches or trust requirements that affect them.
Chosen fix: Add one shared action matrix document centered on the live action gate, family routing policies, and runtime switch interactions so operators can see which intents are real, which are blocked by decision state, and which remain placeholders.
What changed: Added `docs/flow-family-action-matrix.md`, removed Task 343 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `src/email_node/shared_pipeline_core/actions.py`, the family `action_routing.py` adapters, and the family YAML action routing blocks.
###########
Task #344
Step objective: Document all possible cross-family terminal outcomes and operator states, including accept, probation, review_needed, reject, persistence behavior, notification behavior, and user-review handoff.
Chosen fix: Write one shared terminal-outcomes reference built directly from the shared decision and persistence contracts so all families inherit the same operator-visible meaning for trusted, partial, review-needed, and non-persisted results.
What changed: Added `docs/flow-family-terminal-outcomes.md`, removed Task 344 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `src/email_node/shared_pipeline_core/decision.py` and `src/email_node/shared_pipeline_core/persistence.py`.
###########
Task #345
Step objective: Add concrete worked examples for representative emails across multiple families, showing phase-by-phase progression, confidence changes, fallback paths, and final action outcomes.
Chosen fix: Use the existing ORDER and ACTION_REQUIRED runtime report artifacts plus the new-family mailbox sample artifacts to build one worked-examples document that shows the mature accept path, probation fallback path, unknown-to-probation path, and current incomplete family path.
What changed: Added `docs/flow-family-worked-examples.md`, removed Task 345 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against `runtime/order_flow_logs/ad_hoc_reports/order_flow_report.current.index.json`, `runtime/flow_families/action_required/reports/action_required_flow_report.renamecheck.index.json`, and the family sample analysis artifacts.
###########
Task #346
Step objective: Update repo documentation indexes so the new flow and action docs are discoverable from the main runtime, pipeline, and architecture docs.
Chosen fix: Link the new family references from the docs index, runtime doc, shared-core doc, and ORDER pipeline doc so operators and developers can discover them from the main runtime, architecture, and family-specific entrypoints.
What changed: Updated `docs/index.md`, `docs/runtime.md`, `docs/shared-email-pipeline-core.md`, and `docs/Email Processing Pipeline (ORDER Flow).md`, removed Task 346 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: Documentation review completed against the updated entrypoint docs and the newly added family reference set.
###########
Task #347
Step objective: Send a completion user notification over MQTT using the Core-documented user-notification format after the scheduled-task and flow-family work is finished.
Chosen fix: Publish one node-scoped `kind=event` notification request to `hexe/nodes/<node_id>/notify/request` with `targets.external=[\"ha\"]`, then wait briefly on the paired `notify/result` topic so the queue closeout records whether Core confirmed the proxy request.
What changed: Published the completion notification request for node `node-69ecb9b651bb77b4`, removed Task 347 from `docs/New_task.txt`, and appended the completion entry to `docs/completed.txt`.
Verification: MQTT publish command completed successfully. No `notify/result` message arrived during the wait window, so completion is recorded as sent but not Core-confirmed.
###########
