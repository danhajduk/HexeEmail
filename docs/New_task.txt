Workflow skill: implementation workflow

## Codex Execution Directive

You are acting as the Hexe development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

if there is missing data for the task check in the @Core-Documents folder.for core documentation
if it is not present and should be core-owned create a ticket for the req. data in the core dodc
if it is not present and should be node-owned create a file with the new data in the core docs

Standards reference:
- Main standard entrypoint: `/home/dan/Projects/Hexe/docs/standards/Node/README.md`
- Detailed standards folder: `/home/dan/Projects/Hexe/docs/standards/Node/`

Do not edit above this line:
#######################################################################


[WIP] Task 230: Implement template file writer (draft)

- Save generated templates to disk
- Directory: email_node/patterns/draft/

Requirements:
- File name format:
  - {template_id}.json
- Write pretty JSON (indent=2)
- Overwrite behavior:
  - allow overwrite only if explicitly enabled

- Add metadata:
  - add generated_at timestamp (optional if schema allows)
  - DO NOT modify schema fields

Acceptance:
- Template saved correctly
- File readable and valid JSON

[TODO] Task 231: Implement pattern generation service

- Create orchestration service
- File: src/email_node/patterns/pattern_generation_service.py

Flow:
1. Build request model
2. Call AI client
3. Validate response
4. Save template

Return:
- success: template_id, file_path
- failure: structured error

Acceptance:
- End-to-end flow works from input → file

[TODO] Task 232: Add API endpoint for pattern generation

- Add internal endpoint for triggering pattern creation
- File: src/email_node/api/patterns.py

Route:
POST /api/patterns/generate

Input:
- request model from Task 226

Output:
- success:
  - template_id
  - file_path
- error:
  - validation or generation error

Acceptance:
- Endpoint triggers full flow
- Returns deterministic result

[TODO] Task 233: Add CLI tool for pattern generation

- Add CLI entry point
- File: scripts/generate_pattern.py

Usage:
python generate_pattern.py --input sample.json

Requirements:
- Load sample email JSON
- Call pattern generation service
- Print:
  - success + file path
  - or error

Acceptance:
- CLI works end-to-end
- Useful for batch testing

[TODO] Task 234: Add logging and diagnostics

- Add structured logs for:
  - request payload (safe fields only)
  - raw AI response (truncated)
  - validation errors
  - saved file path

Requirements:
- No sensitive data leakage
- Add debug mode for full response capture

Acceptance:
- Logs allow debugging failed generations

[TODO] Task 235: Add initial test coverage

- Create tests for:
  - valid generation flow
  - invalid JSON response
  - schema validation failure
  - file write success

Files:
tests/test_pattern_generation.py

Acceptance:
- Tests cover success + failure paths
- No silent failures
