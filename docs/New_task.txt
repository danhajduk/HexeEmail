Workflow skill: implementation workflow

## Codex Execution Directive

You are acting as the Hexe development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

if there is missing data for the task check in the @Core-Documents folder.for core documentation
if it is not present and should be core-owned create a ticket for the req. data in the core dodc
if it is not present and should be node-owned create a file with the new data in the core docs

Standards reference:
- Main standard entrypoint: `/home/dan/Projects/Hexe/docs/standards/Node/README.md`
- Detailed standards folder: `/home/dan/Projects/Hexe/docs/standards/Node/`

Do not edit above this line:
#######################################################################
[TODO] Task 276: Extract Phase 5 validation into shared validation framework

- Refactor validation/scoring into a family-agnostic framework with loadable policies

Requirements:
- Shared validator must support:
  - required field checks
  - field format checks
  - consistency checks
  - confidence downgrade reasons
- Validation policy loaded by family
- Preserve ORDER downgrade diagnostics where practical

Acceptance:
- ORDER Phase 5 runs through shared validator
- Validator accepts family-specific policies cleanly

[TODO] Task 277: Create family-specific validation policies

- Externalize validation rules from code

ORDER policy examples:
- required fields by profile
- amount/date/order number validation
- tracking consistency checks

ACTION_NEEDED policy examples:
- required action_type/action_url/deadline rules
- amount_due/date/target validation
- security-sensitive handling flags

Acceptance:
- Shared validation framework can load family-specific policy files
- ORDER validation no longer depends on embedded-only rule logic

[TODO] Task 278: Extract Phase 6 decisioning into shared decision framework

- Refactor the new decision engine so it becomes family-driven instead of ORDER-specific

Requirements:
- Shared decision framework must support:
  - accept
  - probation
  - reject
- Optional family-specific extensions allowed later
- Decision policy loaded by family
- Preserve existing ORDER trust boundary behavior

Acceptance:
- ORDER Phase 6 runs through shared decision framework
- Framework is ready for ACTION_NEEDED-specific policy differences

[TODO] Task 279: Create family-specific decision policy packs

- Externalize trust/action thresholds by flow family

ORDER examples:
- high/medium/low thresholds
- probation restrictions
- downstream action eligibility

ACTION_NEEDED examples:
- standard accept/probation/reject policy
- optional sensitive-action handling flags
- persistence and reminder restrictions

Acceptance:
- Decision framework loads family policy packs successfully
- ORDER policy remains equivalent to current behavior

[TODO] Task 280: Extract Phase 7 persistence and action gating into shared framework

- Generalize persistence gate, action gate, and action routing interfaces

Requirements:
- Shared framework handles:
  - persistence gating
  - trust level tagging
  - action authorization
  - action intent routing interface
- Family-specific action routes loaded separately
- Keep ORDER action behavior unchanged

Acceptance:
- ORDER Phase 7 runs through shared framework
- ACTION_NEEDED can later define its own action intents without duplicating the framework

[TODO] Task 281: Create family-specific action routing policies

- Externalize action routing rules by flow family

ORDER examples:
- store_order_record
- update_order_status
- attach_tracking_reference
- user_notification

ACTION_NEEDED examples:
- store_action_record
- user_notification
- queue_reminder
- mark_high_priority
- mark_for_manual_review

Acceptance:
- Shared action router can load family-specific action policies
- ORDER action intents remain stable after migration

[TODO] Task 282: Extract shared report builder for all flow families

- Move report assembly into one shared diagnostics/report module

Requirements:
- Preserve existing per-phase reporting structure:
  - status
  - diagnostics
  - decision
  - persistence
  - actions
- Add flow_family field to reports
- Keep ad hoc report compatibility for ORDER

Acceptance:
- ORDER reports are generated through shared builder
- Reports remain readable and comparable across flow families

[TODO] Task 283: Convert ORDER flow into a thin family-specific runner on top of shared core

- Refactor ORDER pipeline into orchestration only

Requirements:
- ORDER runner should:
  - declare flow_family = order
  - call shared phases in order
  - load ORDER family configs/policies/templates
  - return ORDER-specific output schema
- Remove embedded phase logic that now lives in shared core

Acceptance:
- ORDER flow becomes a thin wrapper around shared core
- Existing ORDER test cases continue to pass

[TODO] Task 284: Add ACTION_NEEDED flow skeleton using shared core

- Create initial ACTION_NEEDED runner that uses the same shared framework

Requirements:
- Declare flow_family = action_needed
- Use shared phases:
  - normalize
  - scrub
  - detect profile
  - template execution/probation
  - validation
  - decision
  - persistence/action gating
- Stub family-specific output schema if needed

Acceptance:
- ACTION_NEEDED flow skeleton exists and can run through the shared core with placeholder configs
- No duplicate phase implementation is introduced

[TODO] Task 284: Add ACTION_NEEDED flow skeleton using shared core

- Create initial ACTION_NEEDED runner that uses the same shared framework

Requirements:
- Declare flow_family = action_needed
- Use shared phases:
  - normalize
  - scrub
  - detect profile
  - template execution/probation
  - validation
  - decision
  - persistence/action gating
- Stub family-specific output schema if needed

Acceptance:
- ACTION_NEEDED flow skeleton exists and can run through the shared core with placeholder configs
- No duplicate phase implementation is introduced

[TODO] Task 286: Document shared-core vs family-specific architecture

- Create architecture doc for the split

Must cover:
- what is shared core
- what is flow-family-specific
- config directory layout
- template directory layout
- probation reuse model
- how ORDER uses the new framework
- how ACTION_NEEDED will plug in

Acceptance:
- Documentation clearly explains the split
- Future implementation work can follow the doc without guessing
